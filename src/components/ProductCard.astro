---
const { id, image, title, price } = Astro.props;
---

<div class="bg-white rounded-lg shadow-md p-4 flex flex-col items-center text-center hover:shadow-xl transition">
  <img src={image} alt={title} class="w-full h-48 object-cover mb-4 rounded" />
  <h3 class="text-lg font-semibold mb-2">{title}</h3>
  <p class="text-purple-600 text-xl font-bold mb-4">${price}</p>
  <a href={`/producto/${id}`} class="bg-purple-600 hover:bg-purple-700 text-white py-2 px-4 rounded">
  Ver más
</a>

</div>
